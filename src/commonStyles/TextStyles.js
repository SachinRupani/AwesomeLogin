import {StyleSheet} from 'react-native';

export const textStyles = StyleSheet.create({
  errorText: {
    fontSize: 13,
  },
  inputText: {
    fontSize: 16,
  },
  buttonText: {
    fontSize: 16,
  },
});
